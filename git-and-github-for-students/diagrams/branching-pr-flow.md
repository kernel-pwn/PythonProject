# Branching & PR flow (Mermaid gitGraph)

An example gitGraph showing branch, commits, and merge.

```mermaid
gitGraph
  commit
  branch feature
  checkout feature
  commit
  commit
  checkout main
  commit
  merge feature
  commit
```
